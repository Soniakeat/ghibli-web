'use strict';

class User { //el repeat password no lo usamos para crear el usuario, sólo para validar
  constructor(name, email, password) {
    this.name = name;
    this.email = email;
    this.password = password;
  }
}

